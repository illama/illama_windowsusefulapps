<Window x:Class="SystemManagerPro.Dialogs.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmation" Height="220" Width="440"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="NoResize"
        Background="{StaticResource BgElevated}" Foreground="{StaticResource TextPrimary}"
        FontFamily="Segoe UI" AllowsTransparency="False">
    <Border BorderBrush="{StaticResource BorderStrong}" BorderThickness="1">
        <Grid Margin="26,24,26,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">
                <Border x:Name="IconBadge" Width="34" Height="34" CornerRadius="17" Background="{StaticResource Warning}">
                    <TextBlock x:Name="IconText" Text="!" FontSize="16" FontWeight="Bold" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock x:Name="TitleText" Text="Confirmer l'action" FontSize="16" FontWeight="SemiBold"
                           VerticalAlignment="Center" Margin="12,0,0,0"/>
            </StackPanel>

            <TextBlock x:Name="MessageText" Grid.Row="1" Text="Êtes-vous sûr ?" TextWrapping="Wrap"
                       Foreground="{StaticResource TextSecondary}" Margin="0,16,0,0" FontSize="13"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                <Button x:Name="CancelBtn" Content="Annuler" Style="{StaticResource BtnGhost}" Click="Cancel_Click" Margin="0,0,8,0"/>
                <Button x:Name="OkBtn" Content="Confirmer" Style="{StaticResource BtnPrimary}" Click="Ok_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
