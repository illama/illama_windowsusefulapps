<UserControl x:Class="SystemManagerPro.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded">
    <ScrollViewer Style="{StaticResource CleanScrollViewer}">
        <StackPanel Margin="32,26,32,26">

            <TextBlock Text="Tableau de bord" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="Vue d'ensemble de l'état de votre système, en temps réel." Style="{StaticResource PageSubtitle}"/>

            <!-- Cartes statistiques -->
            <UniformGrid Columns="4" Margin="0,22,0,0">
                <Border x:Name="CpuCard" Style="{StaticResource CardHover}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="PROCESSEUR" Style="{StaticResource Muted}" FontWeight="SemiBold"/>
                        <TextBlock x:Name="CpuValue" Text="—" FontSize="30" FontWeight="Bold" Margin="0,6,0,10"
                                   Foreground="{StaticResource TextPrimary}"/>
                        <ProgressBar x:Name="CpuBar" Value="0" Maximum="100"/>
                        <Border Height="30" Margin="0,10,0,0" ClipToBounds="True">
                            <Polyline x:Name="CpuSparkline" Stroke="{StaticResource Accent}" StrokeThickness="1.6" StrokeLineJoin="Round"/>
                        </Border>
                    </StackPanel>
                </Border>
                <Border x:Name="RamCard" Style="{StaticResource CardHover}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="MÉMOIRE" Style="{StaticResource Muted}" FontWeight="SemiBold"/>
                        <TextBlock x:Name="RamValue" Text="—" FontSize="30" FontWeight="Bold" Margin="0,6,0,10"/>
                        <ProgressBar x:Name="RamBar" Value="0" Maximum="100"/>
                        <TextBlock x:Name="RamDetail" Style="{StaticResource Muted}" Margin="0,6,0,0"/>
                        <Border Height="30" Margin="0,8,0,0" ClipToBounds="True">
                            <Polyline x:Name="RamSparkline" Stroke="{StaticResource Accent2}" StrokeThickness="1.6" StrokeLineJoin="Round"/>
                        </Border>
                    </StackPanel>
                </Border>
                <Border x:Name="DiskCard" Style="{StaticResource CardHover}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="DISQUE SYSTÈME" Style="{StaticResource Muted}" FontWeight="SemiBold"/>
                        <TextBlock x:Name="DiskValue" Text="—" FontSize="30" FontWeight="Bold" Margin="0,6,0,10"/>
                        <ProgressBar x:Name="DiskBar" Value="0" Maximum="100"/>
                        <TextBlock x:Name="DiskDetail" Style="{StaticResource Muted}" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>
                <Border x:Name="UptimeCard" Style="{StaticResource CardHover}">
                    <StackPanel>
                        <TextBlock Text="DISPONIBILITÉ" Style="{StaticResource Muted}" FontWeight="SemiBold"/>
                        <TextBlock x:Name="UptimeValue" Text="—" FontSize="22" FontWeight="Bold" Margin="0,6,0,10"/>
                        <TextBlock Text="depuis le dernier démarrage" Style="{StaticResource Muted}"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Infos système -->
                <Border Grid.Column="0" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Informations système" Style="{StaticResource SectionTitle}"/>
                        <StackPanel Margin="0,14,0,0" x:Name="InfoStack"/>
                    </StackPanel>
                </Border>

                <!-- Disques -->
                <Border Grid.Column="2" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Disques" Style="{StaticResource SectionTitle}"/>
                        <StackPanel Margin="0,14,0,0" x:Name="DisksStack"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Actions rapides -->
            <Border Style="{StaticResource Card}" Margin="0,14,0,0">
                <StackPanel>
                    <TextBlock Text="Actions rapides" Style="{StaticResource SectionTitle}"/>
                    <WrapPanel Margin="0,14,0,0">
                        <Button Content="🧹  Vider le cache DNS" Style="{StaticResource BtnBase}" Margin="0,0,10,10" Click="FlushDns_Click"/>
                        <Button Content="🛡️  Créer un point de restauration" Style="{StaticResource BtnBase}" Margin="0,0,10,10" Click="RestorePoint_Click"/>
                        <Button Content="📋  Copier le rapport système" Style="{StaticResource BtnBase}" Margin="0,0,10,10" Click="CopyReport_Click"/>
                        <Button Content="🔄  Rafraîchir" Style="{StaticResource BtnPrimary}" Margin="0,0,10,10" Click="Refresh_Click"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
