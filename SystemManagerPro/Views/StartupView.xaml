<UserControl x:Class="SystemManagerPro.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_Loaded">
    <Grid Margin="32,26,32,26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
            <StackPanel>
                <TextBlock Text="Applications au démarrage" Style="{StaticResource PageTitle}"/>
                <TextBlock Text="Registre Run, tâches planifiées et dossier Démarrage." Style="{StaticResource PageSubtitle}"/>
            </StackPanel>
        </StackPanel>

        <!-- Barre d'outils -->
        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Style="{StaticResource Input}" MaxWidth="360" HorizontalAlignment="Left"
                     TextChanged="SearchBox_TextChanged" Tag="Rechercher une application…"/>
            <Button Grid.Column="1" Content="🔄  Actualiser" Style="{StaticResource BtnBase}" Margin="10,0,0,0" Click="Refresh_Click"/>
            <Button Grid.Column="2" Content="＋  Ajouter" Style="{StaticResource BtnPrimary}" Margin="10,0,0,0" Click="Add_Click"/>
        </Grid>

        <!-- Chips statistiques -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0" x:Name="StatsPanel"/>

        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,16,0,0" Padding="0">
            <DataGrid x:Name="AppsGrid" Margin="6">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="NOM" Binding="{Binding Nom}" Width="1.4*"/>
                    <DataGridTextColumn Header="TYPE" Binding="{Binding TypeLabel}" Width="1*"/>
                    <DataGridTextColumn Header="EMPLACEMENT" Binding="{Binding Emplacement}" Width="1.6*"/>
                    <DataGridTextColumn Header="CHEMIN" Binding="{Binding Chemin}" Width="2.4*"/>
                    <DataGridTemplateColumn Header="ACTIF" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding Actif, Mode=OneWay}"
                                          Click="ToggleActive_Click" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
