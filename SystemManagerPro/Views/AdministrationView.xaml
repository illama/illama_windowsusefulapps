<UserControl x:Class="SystemManagerPro.Views.AdministrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_Loaded">
    <ScrollViewer Style="{StaticResource CleanScrollViewer}">
        <StackPanel Margin="32,26,32,26">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Administration" Style="{StaticResource PageTitle}"/>
                    <TextBlock Text="Génération de licences et déblocage total — réservé au compte administrateur." Style="{StaticResource PageSubtitle}"/>
                </StackPanel>
                <Button Grid.Column="1" Content="🚪  Se déconnecter" Style="{StaticResource BtnGhost}" Click="Logout_Click" VerticalAlignment="Top"/>
            </Grid>

            <Border Style="{StaticResource Card}" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="Générer une licence" Style="{StaticResource SectionTitle}"/>
                    <Grid Margin="0,16,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="1.4*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Nom du client" Style="{StaticResource Muted}" Margin="0,0,0,6"/>
                            <TextBox x:Name="CustomerBox" Style="{StaticResource Input}" Tag="Ex : Jean Dupont"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="PC autorisés" Style="{StaticResource Muted}" Margin="0,0,0,6"/>
                            <TextBox x:Name="MaxPcsBox" Style="{StaticResource Input}" Text="1"/>
                        </StackPanel>
                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Durée" Style="{StaticResource Muted}" Margin="0,0,0,6"/>
                            <ComboBox x:Name="DurationCombo" Style="{StaticResource Combo}" SelectedIndex="1">
                                <ComboBoxItem Content="Illimitée"/>
                                <ComboBoxItem Content="30 jours"/>
                                <ComboBoxItem Content="90 jours"/>
                                <ComboBoxItem Content="1 an"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="Le nombre de PC est informatif (suivi de ton côté) : sans serveur central, il n'est pas techniquement imposé."
                               Style="{StaticResource Muted}" Margin="0,10,0,0" TextWrapping="Wrap"/>
                    <Button x:Name="GenerateBtn" Content="✨  Générer la licence" Style="{StaticResource BtnPrimary}" HorizontalAlignment="Left" Margin="0,14,0,0" Click="Generate_Click"/>

                    <Border x:Name="ResultPanel" Background="{StaticResource BgElevated2}" CornerRadius="8" Padding="14" Margin="0,16,0,0" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="Clé générée — copie-la et transmets-la au client" Style="{StaticResource Muted}" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="GeneratedKeyBox" Style="{StaticResource Input}" IsReadOnly="True" FontFamily="Consolas"/>
                                <Button Grid.Column="1" Content="📋 Copier" Style="{StaticResource BtnBase}" Margin="8,0,0,0" Click="CopyGenerated_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,14,0,0">
                <StackPanel>
                    <TextBlock Text="Licences émises (sur ce PC)" Style="{StaticResource SectionTitle}"/>
                    <ItemsControl x:Name="LedgerList" Margin="0,14,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,14,0,0">
                <StackPanel>
                    <TextBlock Text="Licence de cet appareil" Style="{StaticResource SectionTitle}"/>
                    <TextBlock x:Name="DeviceLicenseText" Style="{StaticResource Muted}" Margin="0,10,0,0" TextWrapping="Wrap"/>
                    <Button Content="🗑  Retirer la licence de cet appareil" Style="{StaticResource BtnDanger}"
                            HorizontalAlignment="Left" Margin="0,12,0,0" Click="RemoveDeviceLicense_Click"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
