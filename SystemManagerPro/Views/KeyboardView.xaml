<UserControl x:Class="SystemManagerPro.Views.KeyboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_Loaded">
    <ScrollViewer Style="{StaticResource CleanScrollViewer}">
        <StackPanel Margin="32,26,32,26">
            <TextBlock Text="Remapping du clavier" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="Remappe des touches au niveau système via la Scancode Map (nécessite un redémarrage)." Style="{StaticResource PageSubtitle}"/>

            <Border Style="{StaticResource Card}" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="Créer un mapping" Style="{StaticResource SectionTitle}"/>
                    <Grid Margin="0,14,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Touche source" Style="{StaticResource Muted}" Margin="0,0,0,6"/>
                            <ComboBox x:Name="SourceCombo" Style="{StaticResource Combo}" DisplayMemberPath="Name"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="→" FontSize="18" Foreground="{StaticResource TextSecondary}"
                                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10"/>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Touche destination" Style="{StaticResource Muted}" Margin="0,0,0,6"/>
                            <ComboBox x:Name="DestCombo" Style="{StaticResource Combo}" DisplayMemberPath="Name"/>
                        </StackPanel>
                        <Button Grid.Column="3" Content="＋  Ajouter" Style="{StaticResource BtnBase}"
                                VerticalAlignment="Bottom" Margin="12,0,0,0" Click="AddMapping_Click"/>
                    </Grid>

                    <TextBlock Text="Mappings en attente d'application" Style="{StaticResource SectionTitle}" Margin="0,20,0,10"/>
                    <ItemsControl x:Name="PendingList"/>
                    <TextBlock x:Name="PendingEmptyLabel" Text="Aucun mapping en attente." Style="{StaticResource Muted}"/>

                    <Button Content="✅  Appliquer les mappings ci-dessus" Style="{StaticResource BtnPrimary}"
                            HorizontalAlignment="Left" Margin="0,16,0,0" Click="ApplyMappings_Click"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,14,0,0">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Mapping actuellement actif sur ce PC" Style="{StaticResource SectionTitle}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="🔄  Actualiser" Style="{StaticResource BtnBase}" Click="RefreshCurrent_Click" Margin="0,0,8,0"/>
                            <Button Content="🗑  Tout supprimer" Style="{StaticResource BtnDanger}" Click="RemoveAll_Click"/>
                        </StackPanel>
                    </Grid>
                    <ItemsControl x:Name="CurrentList" Margin="0,14,0,0"/>
                    <TextBlock x:Name="CurrentEmptyLabel" Text="Aucun remapping actif." Style="{StaticResource Muted}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
