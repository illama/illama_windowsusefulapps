<UserControl x:Class="SystemManagerPro.Views.LicenseActivationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Border Style="{StaticResource Card}" MaxWidth="440" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30">
            <StackPanel>
                <TextBlock Text="🔒" FontSize="34" HorizontalAlignment="Center"/>
                <TextBlock x:Name="TitleText" Text="Fonctionnalité verrouillée" Style="{StaticResource PageTitle}"
                           FontSize="19" HorizontalAlignment="Center" Margin="0,14,0,0"/>
                <TextBlock x:Name="MessageText" Text="Cette fonctionnalité nécessite une licence."
                           Style="{StaticResource PageSubtitle}" TextAlignment="Center" HorizontalAlignment="Center" Margin="0,8,0,0"/>

                <TextBlock Text="Clé de licence" Style="{StaticResource Muted}" Margin="0,24,0,6"/>
                <TextBox x:Name="KeyBox" Style="{StaticResource Input}" Tag="Colle ta clé de licence ici…"/>
                <Button x:Name="ActivateBtn" Content="🔓  Activer" Style="{StaticResource BtnPrimary}" HorizontalAlignment="Stretch"
                        Margin="0,12,0,0" Click="Activate_Click"/>
                <TextBlock x:Name="StatusText" Style="{StaticResource Muted}" Margin="0,10,0,0" TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center"/>

                <Border BorderBrush="{StaticResource BorderBrush2}" BorderThickness="0,1,0,0" Margin="0,20,0,16"/>
                <Button x:Name="AdminLoginBtn" Content="🛡️  Connexion administrateur" Style="{StaticResource BtnGhost}" HorizontalAlignment="Center" Click="AdminLogin_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
